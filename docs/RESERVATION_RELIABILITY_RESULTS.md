# Court reservation reliability

Verified 2026-09-25 against the customer and staff booking flows and Supabase project `xrlwtnwamboucihsamrr`. The deployed design uses immutable listing/unit IDs, a shared physical-resource ledger, and a GiST exclusion constraint over physical resource and half-open time ranges. Customer and walk-in writes still pass through their existing RLS and payment rules.

## Results

| Check | Result |
| --- | --- |
| Same physical court cannot be reserved twice at overlapping times, including across sports/channels | Rollback-only live source-table test passed on the deployed partition map: simultaneous Pickleball Courts 1 and 2 were accepted; an overlapping Basketball Court 2 walk-in was rejected with `23P01`. The two-request live race also rejected the competing walk-in. |
| A different physical court remains available at the same time | In the same rollback-only live test, Lawn Tennis Court 3 was accepted at the same time as Pickleball Courts 1 and 2 on other physical surfaces. |
| Blank/stale unit labels do not change unit identity | Live SQL test passed: valid unit UUIDs canonicalize the label; an overlapping request against the same resource fails while another unit succeeds. |
| Availability follows saved reservations and listing renames | Live SQL test passed: after a temporary listing rename and booking confirmation, authenticated `court_occupancy()` returned the occupied unit under the current listing name. The rename was rolled back. |
| Mapping/inventory edits preserve existing physical holds | Live SQL test passed for rejection of mapping removal during an active booking. Refresh now rejects any proposed configuration that would remove an existing active `(source, resource)` pair. |
| Active resource/listing cannot be retired under active reservations | Live SQL test passed for both deactivation attempts; both were rejected with `23503`. The guard includes past-due source rows whose status is still pending/confirmed. |
| Checkout retry and payment rules | Live SQL test passed: an authorized saved attempt remained retrievable after the court rate was cleared and status changed to Maintenance; an unauthorized owner failed, and a new attempt without an Available hourly rate was rejected. Existing webhook tests pass 7/7. |
| Customer and staff browser booking behavior | Headless Edge fixture passed for customer multi-item dates/courts, staff walk-in pricing and saves, availability refresh, partial-save retry, wildcard legacy rows, conflicts, errors, stale responses, saved staff schedule, failed inventory reads, and responsive widths. The capacity fixture verifies that a full Basketball Court 2 blocks all three mapped Pickleball subcourts while separate Pickleball subcourts remain independently selectable. |
| Public and customer rate display | Landing-page browser test reads live per-unit schedules and confirms Basketball shows a starting price, its viewer changes from Court 1 day/night pricing to Court 2's rate, Bowling shows its per-set starting price, and unpriced Pickleball stays TBA. Customer booking selectors use the same per-unit starting-rate hint; the actual quote still comes from the selected unit and time. |
| Owner shared-resource controls | Headless Edge fixture passed for creating a physical resource and mapping a sport unit to share it with another listing. |
| Live Pickleball shared-surface availability | With an authenticated claim set only for the rollback-only read test, `court_occupancy()` projected the existing pending Basketball Court 2 reservation as occupied on Basketball Court 2 and Pickleball Courts 1, 2, and 3. |

The customer/staff static court fallback now also reflects the verified 10 Pickleball subcourts and shared host-surface description; booking selectors still fail closed if verified live unit inventory cannot be read. The landing-page regression checks this fallback count against the deployed inventory.

Cross-sport, retirement, and checkout behavior tests used temporary changes inside transactions and rolled them back. The concurrent race used a far-future test booking, observed the competing walk-in fail, and immediately cancelled the booking. Production now has the manager-confirmed Pickleball layout: Courts 1–3 share Basketball Court 2, Courts 4–6 share Volleyball Court 1, Courts 7–8 share Lawn Tennis Court 1, and Courts 9–10 share Lawn Tennis Court 2. Each host court maps to all of its Pickleball zones, while each Pickleball unit maps to one zone. The latest live audit reports 7 active sources, 23 resource-slot rows, zero active sources missing physical holds, and zero overlapping physical-resource pairs. A live authenticated `court_occupancy()` read showed the pending Basketball Court 2 booking projected onto Pickleball Courts 1–3. All 10 Pickleball units are verified and mapped; their prices remain unset pending the manager's old/new tier assignment.

## Migrations

The live Supabase history includes the physical-resource inventory, stable unit/listing identity, serialized configuration edits, active-resource retirement guards, preservation of existing holds, refresh de-duplication, stable PayMongo retry handling, and the Pickleball capacity map. See the corresponding dated files in [`supabase/migrations`](../supabase/migrations), including `20260925074618_configure_pickleball_shared_surface_capacity.sql`, `20260925074833_fix_pickleball_tennis_zone_mapping.sql`, and `20260925080000_retire_orphaned_pickleball_resources.sql`.

The rate schedule and authoritative-pricing migrations also run on the live project. Inserts now replace client-supplied totals from the configured unit schedule and save a rate-unit snapshot; walk-ins also save the authoritative paid amount. A follow-up checkout migration recalculates a missing legacy total from current server rates when pricing becomes configured later. A rollback-only live checkout test used a future TBA booking, temporarily set a ₱300/hour listing rate, and confirmed a two-hour downpayment attempt was ₱300 (₱600 total). The transaction rolled back; no payment attempt or rate change remained.

An independent security/QA review identified three pricing display gaps, now fixed: customer checkout verifies the saved amount and asks before proceeding if it differs from the displayed quote; staff receipts use the returned saved amount rather than the cached wizard price; and online retry can calculate an available quote for a future booking whose amount was TBA at creation. The browser fixture now includes a mid-wizard rate-change case for customer checkout and the staff receipt. Existing service-role-only checkout, active-customer ownership checks, RLS, and signed webhook gates were retained.

The customer wizard now supports a booking list with a separate court, physical unit, date, time range, payment option, and estimate for every item. Customers can add one item or save a list across sports. The list prevents an overlapping duplicate when items share a configured physical resource; every saved row is also independently rechecked against live availability, and the database constraint remains authoritative for races. Items are saved as separate bookings and online payments are opened separately from My Bookings. A focused partial-save test confirms that when a later insert returns a conflict after an earlier item saved, the earlier item is retained once and retry saves only the failed item. If the browser loses the response after a server commit, commit status is unknowable to the client; the customer is told to check My Bookings before retrying.

Confirmed rates are seeded on the live per-unit schedules: Basketball Court 1 old ₱700 day/₱1,000 night and Court 2 new ₱1,300 day/night; Badminton Courts 1–5 old ₱250/hr and Courts 6–9 new ₱300/hr; Volleyball ₱500/hr; Lawn Tennis ₱200 day/₱300 night; Billiards ₱250/hr; Table Tennis ₱100/hr; Duckpin ₱100/set; and Ten-Pin ₱150/set. Because the current inventory only labels Basketball and Badminton units numerically, the old/new assignment follows the manager-provided counts in label order and can be adjusted in the owner Court Inventory screen if onsite labels differ. The matching migration is `20260925071136_seed_basketball_badminton_rates.sql`.

Pickleball count and physical sharing are now confirmed and deployed as 10 capacity partitions. Basketball Court 1 old and Court 2 new are confirmed; the corresponding Basketball rates are set to ₱700/₱1,000 day/night and ₱1,300 day/night. Pickleball's separate old/new rate assignment is still pending, so those 10 units display Rate TBA until the manager identifies which are ₱200/hr and which are ₱300/hr. The deployed mapping migrations are `20260925074618_configure_pickleball_shared_surface_capacity.sql` and `20260925074833_fix_pickleball_tennis_zone_mapping.sql`. Since no cutoff choice was received, 18:00 Asia/Manila was applied as an explicit default and can be changed in owner settings. The authoritative quote function was verified at the boundary: Basketball Court 1 quotes ₱700 for 17:00–18:00 and ₱1,000 for 18:00–19:00; Lawn Tennis quotes ₱200 and ₱300 for the same intervals. The deployed cutoff migration is `20260925073331_set_night_rate_cutoff_18_00.sql`.

Independent read-only QA verified the exact ten-zone map, host-to-subcourt occupancy projection, customer/staff booking fixture, public starting-rate display, and payment/access controls. It found two obsolete Pickleball resource rows with no unit mappings or reservation holds; migration `20260925080000_retire_orphaned_pickleball_resources.sql` now retires those exact rows only when both checks remain clear. Migration `20260925081000_restrict_staff_role_helpers.sql` also removed anonymous execution of two SECURITY DEFINER role-check helpers while preserving authenticated and service-role access. Live privilege and policy checks passed. A post-migration live audit reports 7 active sources, 23 resource-slot rows, zero missing holds, zero overlaps, 10 verified Pickleball units, zero priced Pickleball units, and zero active obsolete resources.

## Reproduce the UI and payment checks

Start the preview server with `node scripts/preview.cjs`, then run:

```powershell
$env:PLAYWRIGHT_MODULE='C:/Users/Kiko/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'
node tests/reservation-qa-ui.cjs
node tests/paymongo-webhook.test.mjs
```

Results: reservation UI fixture passed, including customer multi-booking and partial retry, staff walk-ins, sport-scoped owner unit rates/status/add-unit count/photo-slot updates, capacity-safe cross-sport availability links, rate-change receipt/checkout checks, shared-resource conflicts, and responsive widths; PayMongo webhook/expiry suite passed 7/7. `node --check` passed for the changed JavaScript files and `git diff --check` reported no whitespace errors (Git notes line-ending normalization warnings for a few edited files).

## Concurrency and test limits

The live concurrency test held the first insert transaction open for seven seconds while a second session attempted the same physical court. The second request waited on the shared configuration lock and failed with `23P01`. The temporary booking was cancelled immediately. The older disposable PostgreSQL runner still targets the prior text-ledger migration rather than the new physical-resource migrations.

## Owner court editor redesign follow-up

The owner page no longer presents a global physical-resource console. Rates, billing basis, pricing tier, and bookable status are managed on each court unit inside that sport's Edit view. Availability choices are shown as the other sport/unit names. For a larger court covering several smaller courts, each smaller court remains a separate selectable space; the larger court is the place to manage those capacity links. Smaller units cannot accidentally join a sibling's space, and links owned by a multi-space court stay read-only from the smaller court's editor. New units receive a dedicated availability space and update the listing count and photo-slot state before the open listing can be saved.

Independent QA initially found unsafe sibling-link options, an unlocked host-owned checkbox after rate saves, stale count/photo state after adding a unit, repeated suggested labels, and success messages for zero-row writes. These were fixed and covered by the refreshed owner fixture. The final `tests/reservation-qa-ui.cjs` run passed for owner rates/availability/add-unit flow, customer and staff booking flows, shared-resource overlap handling, and the 320–1440px layout. Static security review found no new authorization bypass: writes still rely on existing RLS, and admin inventory changes require a returned row. This fixture suite mocks Supabase; it does not replace the live overlap/race evidence above. Photo aspect-ratio/quality work remains a later follow-up, and the Pickleball unit rate split remains unconfigured pending the manager's confirmation.
